[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "spn_pr"
version = "0.1.0"
description = "1D-ResNet with MC Dropout"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["src"]